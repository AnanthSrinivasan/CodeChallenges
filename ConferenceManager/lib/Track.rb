class Track
	def initialize(talks_info, sessions_info)
		@talks_info = talks_info
		@sessions_info = sessions_info
	end
	
	def arrange
		# value = 0 
		# keysToDelete = []
		# contents.talks_data.each_pair { |key, val|  
		# 	# puts "#{key} , #{val}"
		# 	value += val.split('min')[0].to_i
		# 	keysToDelete << key if value <= 180
		# }

		# keysToDelete.each {|key| 
		# 	puts key
		# 	contents.talks_data.delete(key) 
		# }		
	end
	
end